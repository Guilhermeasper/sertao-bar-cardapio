import { SBSectionItemsPipe } from './section-items.pipe';

describe('SectionItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SBSectionItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
