import { SectionItemsPipe } from './section-items.pipe';

describe('SectionItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SectionItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
